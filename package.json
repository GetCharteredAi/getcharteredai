{
  "name": "getcharteredai-platform",
  "version": "1.0.0",
  "description": "Get Chartered AI — APC Learning Platform",
  "scripts": {
    "validate": "node scripts/validate-links.mjs && node scripts/validate-index-js.mjs",
    "smoke": "npm run validate"
  }
}
